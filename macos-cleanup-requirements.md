# macOS 存储清理工具 — 设计优化需求文档

> 基于《macOS 存储清理指南》升级而来
> 目标：将系统存储控制在 100GB 以下，解决卡顿与缓存堆积问题
> 文档性质：产品设计需求规格（PRD），用于指导后续开发
> 最后更新：2026-09-04

---

## 一、问题与目标

### 1.1 现状痛点

| 痛点 | 表现 | 根因 |
|------|------|------|
| 电脑卡顿 | 启动慢、切换卡、编译等待长 | 缓存与冗余数据堆积，磁盘空间不足 |
| 缓存大量堆积 | `~/Library/Caches`、`~/.npm`、`~/.gradle` 等动辄几十 GB | 缺乏定期清理机制，手动敲命令成本高 |
| 清理风险高 | 不确定哪些能删、删错后果 | 原文档是静态 bash 清单，无确认、无预览、无反馈 |
| 无法量化 | 清理完不知道释放了多少空间 | 原命令执行后只显示 `rm` 成功，无空间回显 |

### 1.2 设计目标

1. **分级可控**：诊断 / 一级（零风险）/ 二级（低风险）/ 三级（中风险），四个独立菜单，风险一目了然
2. **粒度到单个命令**：每个清理项是独立开关，可勾选、可单独确认执行，不做"一键全删"
3. **确认先行**：每个命令执行前必须有明确确认动作，禁止无确认批量执行
4. **实时反馈**：每个命令执行后立即显示清理前/后大小、释放空间、执行状态
5. **可度量**：主界面实时展示当前磁盘占用、各级预估回收、累计已释放空间

---

## 二、产品定位与形态

### 2.1 产品名称（建议）

`macOS Cleanup Hub`（或沿用项目命名风格，暂定英文名，UI 文案用中文）

### 2.2 形态选择（二选一，推荐方案 A）

| 方案 | 描述 | 优点 | 缺点 |
|------|------|------|------|
| **A. 桌面 GUI（Tauri + SolidJS）** | 原生感强，图形化菜单，实时进度条，适合鼠标操作 | 与当前技术栈（Tauri/SolidJS/Tailwind）完全契合；可复用现有构建与 GitHub Action；体验最好 | 开发成本较高 |
| B. 交互式 TUI（终端菜单） | 用 `dialog`/`fzf`/`crossterm` 做终端菜单 | 开发快，纯命令行 | 反馈粒度弱，无图形进度 |

**推荐方案 A**，并提供「高级模式」切换到原始 bash 命令输出，满足命令行用户习惯。

---

## 三、总体功能架构

```
macOS Cleanup Hub
│
├── 0. 顶部状态栏
│   ├── 磁盘总占用（实时，GB）
│   ├── 可用空间
│   ├── 目标线：100GB（红色警告线）
│   └── 本次会话累计释放
│
├── 1. 主菜单（四级入口）
│   ├── 🩺 诊断总览      → 只读，展示占用排行
│   ├── 🟢 一级清理      → 零风险，全是缓存，可直接执行
│   ├── 🟡 二级清理      → 低风险，需重建索引/缓存
│   └── 🔴 三级清理      → 中风险，IDE 冗余数据，按需删除
│
├── 2. 每级子菜单（核心）
│   ├── 左侧：命令列表（可勾选，每项显示预估大小与风险标签）
│   └── 右侧/底部：操作区（预览 → 确认 → 执行 → 反馈）
│
├── 3. 执行反馈区
│   ├── 清理前大小
│   ├── 清理后大小
│   ├── 释放空间（高亮数字）
│   ├── 执行状态（成功/失败/跳过）
│   └── 异常说明（如需 sudo、依赖缺失）
│
└── 4. 设置
    ├── 目标磁盘线（默认 100GB，可调）
    ├── 是否需要 sudo 提示
    ├── 排除列表（用户自定义不删路径）
    └── 日志导出
```

---

## 四、分级菜单详细设计

> 每个级别都是**独立菜单**，从主菜单进入。级别内每个命令是**独立可勾选项**。

### 4.0 诊断总览菜单（只读，不清理）

**进入路径**：主菜单 → 🩺 诊断总览

**功能**：扫描并展示当前磁盘占用结构，帮助用户判断"钱花在哪里"。**纯只读，无删除动作。**

| 序号 | 命令 | 作用 | 输出展示 |
|------|------|------|----------|
| D1 | `df -h /` | 磁盘总览 | 总量/已用/可用/使用率进度条 |
| D2 | `du -sh ~/*` | 用户目录大文件排行 | Top 20 表格，含大小与路径 |
| D3 | `du -sh ~/.*` | 隐藏文件排行 | Top 20 表格 |
| D4 | `du -sh ~/Library/Caches/*` | 缓存排行 | Top 20 表格，红色高亮 >1GB 项 |
| D5 | `du -sh ~/Library/Application Support/*` | 应用支持数据排行 | Top 20 表格 |
| D6 | `du -sh ~/Desktop ~/Documents ~/Downloads ~/Movies ~/Music ~/Pictures` | 可见目录汇总 | 柱状图 |

**交互**：
- 顶部「一键全跑」按钮：顺序执行全部 6 个诊断命令，汇总成一份报告
- 每个命令也可单独点击执行
- 报告支持「导出为文本」

---

### 4.1 一级清理菜单（零风险）

**进入路径**：主菜单 → 🟢 一级清理（完全安全，直接执行）

**风险标签**：🟢 零风险 — 全是缓存，删除后自动重建，不影响数据

**预估回收**：~50 GB

**命令列表（每项独立勾选 + 确认键）**：

| ID | 命令 | 路径/操作 | 预估大小 | 风险 | 说明 |
|----|------|-----------|----------|------|------|
| L1-1 | npm 缓存 | `rm -rf ~/.npm/_cacache` | ~15GB | 零 | 包管理缓存，重建快 |
| L1-2 | bun 缓存 | `rm -rf ~/.bun/install/cache` | ~2GB | 零 | 包管理缓存 |
| L1-3 | pip 缓存 | `rm -rf ~/Library/Caches/pip` | ~1GB | 零 | Python 包缓存 |
| L1-4 | Yarn 缓存 | `rm -rf ~/Library/Caches/Yarn/v6` | ~1GB | 零 | 包管理缓存 |
| L1-5 | pnpm 缓存 | `rm -rf ~/Library/Caches/pnpm` | ~3GB | 零 | 包管理缓存 |
| L1-6 | 通用 cache | `rm -rf ~/.cache/*` | ~5GB | 零 | 系统通用缓存 |
| L1-7 | TypeScript 缓存 | `rm -rf ~/Library/Caches/typescript` | ~2GB | 零 | TS 编译缓存 |
| L1-8 | node-gyp 缓存 | `rm -rf ~/Library/Caches/node-gyp` | ~1GB | 零 | native module 构建缓存 |
| L1-9 | Go 构建缓存 | `rm -rf ~/Library/Caches/go-build` | ~3GB | 零 | Go 缓存 |
| L1-10 | Google 缓存 | `rm -rf ~/Library/Caches/Google` | ~2GB | 零 | Chrome/Google 系应用缓存 |
| L1-11 | Homebrew 缓存 | `rm -rf ~/Library/Caches/Homebrew` | ~3GB | 零 | brew 缓存 |
| L1-12 | JetBrains 缓存 | `rm -rf ~/Library/Caches/JetBrains` | ~3GB | 零 | IDE 索引缓存 |
| L1-13 | GeoServices 缓存 | `rm -rf ~/Library/Caches/GeoServices` | ~1GB | 零 | 地理服务缓存 |
| L1-14 | 应用更新缓存 | `rm -rf ~/Library/Application Support/Caches/*` | ~3GB | 零 | macOS 应用更新暂存 |
| L1-15 | Docker 清理 | `docker system prune -a -f` | ~5GB | 零 | 容器/镜像/卷残留 |
| L1-16 | Homebrew 旧版本 | `brew cleanup --prune=all` | ~3GB | 零 | 旧 formula/cask |

**交互流程（每个命令统一）**：
```
[勾选 L1-1 npm 缓存]  预估释放 ~15GB  [预览] [执行]
                                                    │
                                                    ▼
                                          ┌─ 确认弹窗 ─┐
                                          │ 删除后将自动重建    │
                                          │ 确认  取消        │
                                          └──────────────┘
                                                    │
                                          ┌─ 执行中 ─┐
                                          │ ● 清理中… │
                                          └───────────┘
                                                    │
                                          ┌─ 完成反馈 ─┐
                                          │ ✓ 完成      │
                                          │ 清理前 18.2GB│
                                          │ 清理后 3.2GB │
                                          │ 释放 15.0GB  │
                                          └─────────────┘
```

**批量操作**：
- 「全选」/「全不选」
- 「一键执行已勾选」：逐个执行，每个完成后滚动显示反馈，最后汇总

---

### 4.2 二级清理菜单（低风险）

**进入路径**：主菜单 → 🟡 二级清理（检查后执行）

**风险标签**：🟡 低风险 — 删除后需重建索引/缓存，首次运行会变慢，但数据不丢失

**预估回收**：~85 GB

**命令列表**：

| ID | 命令 | 操作 | 预估大小 | 风险 | 重建成本 | 说明 |
|----|------|------|----------|------|----------|------|
| S2-1 | CodeGraph 索引 | `rm -rf ~/.codegraph/codegraph.db` | ~30GB | 低 | 自动重建 | 最大头，下次开项目自动重建 |
| S2-2 | npm 全部缓存 | `rm -rf ~/.npm/*` | ~10GB | 低 | 重建快 | 比一级 L1-1 更彻底 |
| S2-3 | NVM 旧 Node 版本 | `nvm ls` → `nvm uninstall <v>` | ~5GB | 低 | 无 | **交互式**：先列出版本，用户选择保留 2-3 个 |
| S2-4 | Gradle 构建缓存 | `rm -rf ~/.gradle/caches` | ~8GB | 低 | 重建 | Kotlin/Java 项目 |
| S2-5 | Rustup 旧工具链 | `rustup toolchain list` → `remove` | ~5GB | 低 | 无 | **交互式**：先列工具链，用户勾选删除 |
| S2-6 | Cargo 缓存 | `cargo cache --remove-all` 或 `rm -rf ~/.cargo/registry` | ~8GB | 低 | 重建 | Rust 包缓存 |
| S2-7 | Vite 构建缓存 | `rm -rf ~/.vite-plus` | ~3GB | 低 | 重建 | 可能需 sudo，失败时提示用户手动处理 |
| S2-8 | Maven 缓存 | `rm -rf ~/.m2/repository` | ~8GB | 低 | 重建 | Java 项目 |

**特殊交互（S2-3、S2-5）**：
- 这两项是「先查看再决定」，不能直接执行删除
- 点击后弹出二级窗口：列出已安装版本 → 用户勾选要删的 → 确认 → 逐个删除并反馈

**sudo 处理（S2-7）**：
- 执行前检测权限，若不足则在反馈区提示「需 sudo，已暂停，请手动执行或前往设置开启 sudo 模式」

---

### 4.3 三级清理菜单（中风险）

**进入路径**：主菜单 → 🔴 三级清理（IDE 冗余数据，按需删除）

**风险标签**：🔴 中风险 — 删除后下次启动需重建数据，历史配置可能丢失

**预估回收**：~15 GB

**核心原则**：**必须确认当前不使用的 IDE 才能删**。界面上每个 IDE 项应显示「是否在用」检测状态。

| ID | 命令 | 路径 | 预估大小 | 风险 | 检测在用 |
|----|------|------|----------|------|----------|
| T3-1 | Windsurf | `rm -rf ~/.windsurf` | 2.7GB | 中 | 进程检测 |
| T3-2 | Codex | `rm -rf ~/.codex` | 1.8GB | 中 | 进程检测 |
| T3-3 | Codeflicker | `rm -rf ~/.codeflicker` | 1.4GB | 中 | 进程检测 |
| T3-4 | Trae | `rm -rf ~/.trae` | 1.3GB | 中 | 进程检测 |
| T3-5 | Lingma | `rm -rf ~/.lingma` | 1.2GB | 中 | 进程检测 |
| T3-6 | MarsCode | `rm -rf ~/.marscode` | 870MB | 中 | 进程检测 |
| T3-7 | Codeium | `rm -rf ~/.codeium` | 673MB | 中 | 进程检测 |

**安全机制**：
- 每项旁边显示「🟢 在用 / ⚪ 未检测到」状态
- 若检测到该 IDE 进程正在运行，**禁用删除按钮**并提示「请先关闭该 IDE」
- 确认弹窗必须再次明确写出「将删除 <IDE名> 的全部配置与历史，此操作不可逆」

---

## 五、确认与反馈机制（核心需求）

### 5.1 确认键设计规范

**三级确认强度**，按风险自动匹配：

| 风险级别 | 确认方式 | 确认文案要求 |
|----------|----------|--------------|
| 🟢 一级（零风险） | 简单确认弹窗 | 「删除后自动重建，确认执行？」 |
| 🟡 二级（低风险） | 强确认弹窗 | 必须勾选「我已了解需重建索引」才可点击确认 |
| 🔴 三级（中风险） | 最强确认弹窗 | 必须在输入框中输入 IDE 名称（如 `windsurf`）才能确认 |

### 5.2 实时反馈设计

每个命令执行后**立即**在反馈区显示（不等待批量结束）：

```
┌─ L1-1 npm 缓存 ────────────────────────┐
│ 状态     ✓ 完成                          │
│ 清理前   18.2 GB                         │
│ 清理后    3.2 GB                         │
│ 释放     15.0 GB   🎉                   │
│ 耗时       0.8s                          │
└─────────────────────────────────────────┘
```

**反馈区特性**：
- 滚动展示，最新的在顶部
- 释放空间用绿色 + 加粗高亮
- 失败项显示红色 + 失败原因（如权限不足、路径不存在）
- 底部实时汇总：「本次已释放 XX GB / 预估共可释放 XX GB」

### 5.3 进度与动画

- 单个命令执行时：按钮显示进度条 + 「清理中…」
- 批量执行时：顶部显示「已执行 5/16」，每完成一个滚动一条反馈
- 全局磁盘占用状态栏实时更新（每 2s 刷新或清理后立即刷新）

---

## 六、主界面总览设计

### 6.1 顶部状态栏（始终可见）

```
┌─────────────────────────────────────────────────────┐
│  macOS Cleanup Hub            [设置] [日志] [关于]    │
│                                                     │
│  💿  磁盘总览                                        │
│  ████████████░░░░░░░░░░  78%  已用  142GB / 180GB   │
│  ⚠ 超过目标线 100GB  ·  超出 42GB                    │
│                                                     │
│  🎯 本次会话已释放  0 GB    目标可释放  ~150 GB      │
└─────────────────────────────────────────────────────┘
```

### 6.2 四级菜单卡片

```
┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│ 🩺 诊断总览   │  │ 🟢 一级清理   │  │ 🟡 二级清理   │  │ 🔴 三级清理   │
│ 只读·不删除   │  │ 零风险       │  │ 低风险       │  │ 中风险       │
│              │  │              │  │              │  │              │
│ 6 项诊断命令 │  │ 16 个清理项  │  │ 8 个清理项   │  │ 7 个清理项   │
│ 预估释放 0GB  │  │ 预估 ~50GB   │  │ 预估 ~85GB   │  │ 预估 ~15GB   │
│ [进入]       │  │ [进入]       │  │ [进入]       │  │ [进入]       │
└──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘
```

---

## 七、安全与容错设计

### 7.1 安全红线

1. **绝不静默执行**：任何删除命令必须经过用户确认
2. **绝不批量无确认**：即使用户点了「一键执行已勾选」，也要在执行前汇总确认
3. **路径白名单**：所有删除路径必须在预定义白名单内（`~/.npm`、`~/Library/Caches/*` 等），禁止执行用户自定义路径
4. **二次确认不可跳过**：二级、三级的强确认不可勾选「不再提示」
5. **sudo 保护**：需要 sudo 的命令必须单独提示，不自动请求密码

### 7.2 异常处理

| 场景 | 处理 |
|------|------|
| 路径不存在 | 跳过并显示「⚠ 路径不存在，已跳过」，不报错中断 |
| 权限不足 | 显示红色提示 + 给出手动命令，允许继续其他项 |
| 命令执行超时 | 显示「超时，请手动检查」，标记为失败 |
| 磁盘空间不足（清理目标） | 若清理后仍超目标线，底部显示建议进入下一级 |
| 意外退出 | 已执行的命令记录到日志，下次启动可查看 |

### 7.3 排除列表（设置中）

- 用户可添加自定义路径到「永不清理」排除列表
- 排除列表在执行前自动过滤

---

## 八、技术方案建议

### 8.1 推荐技术栈（复用现有项目）

| 层级 | 技术 | 理由 |
|------|------|------|
| 框架 | Tauri 2.0 | 轻量桌面壳，系统命令调用能力强 |
| 前端 | SolidJS + Tailwind CSS | 项目现有技术栈，响应式实时更新友好 |
| 系统调用 | Rust（`std::process::Command`） | 安全执行 shell 命令，捕获输出与退出码 |
| 磁盘扫描 | Rust 调用 `du`/`df`，解析输出 | 比 JS 更高效 |
| 状态管理 | SolidJS Signals | 实时反馈区需要细粒度响应式更新 |
| 持久化 | SQLite 或本地 JSON | 记录清理历史、排除列表、目标线设置 |

### 8.2 命令执行层设计（Rust）

```rust
// 每个清理项定义为结构化命令，而非裸字符串
struct CleanupAction {
    id: &'static str,
    tier: Tier,              // 1/2/3
    name: &'static str,
    estimate_gb: f64,
    risk: Risk,
    commands: Vec<Command>,  // 支持多步骤
    requires_sudo: bool,
    interactive: bool,       // S2-3/S2-5 这类需用户参与选择
}

enum Command {
    Remove(Path),             // rm -rf
    Run(String),              // 系统命令
    Interactive(Interactive), // nvm ls → 用户选择
}
```

**关键收益**：命令结构化后，确认弹窗、预估大小、风险标签、反馈解析都可自动生成，避免硬编码。

### 8.3 开发分期（建议）

| 阶段 | 内容 | 产出 |
|------|------|------|
| P0 | 主菜单 + 一级清理 + 反馈机制 | 可立即使用，覆盖 ~50GB |
| P1 | 二级清理 + 交互式命令（NVM/Rustup） | 覆盖 ~135GB |
| P2 | 三级清理 + IDE 在用检测 | 覆盖 ~150GB |
| P3 | 诊断总览 + 设置 + 日志 | 完整产品 |

---

## 九、验收标准

1. **分级独立**：从主菜单可分别进入四个菜单，互不干扰
2. **粒度确认**：每个清理命令可单独勾选、单独确认、单独执行，不做一刀切
3. **实时反馈**：每个命令执行后 1 秒内显示清理前/后大小与释放空间
4. **风险可见**：每个命令、每个菜单都有清晰的风险等级标签
5. **安全红线**：无确认绝不执行任何删除；二次确认不可跳过
6. **目标可达**：执行完一、二、三级后，磁盘占用可从 ~142GB 降至 ~100GB 以内
7. **可中断**：批量执行中可随时暂停/取消，已执行的不回滚但记录在案

---

## 十、附录

### A. 原文档命令对照表

| 原文档章节 | 本文档对应菜单 | 命令数 |
|------------|----------------|--------|
| 一、诊断命令 | 4.0 诊断总览 | 6 |
| 二、一级清理 | 4.1 一级清理 | 16 |
| 三、二级清理 | 4.2 二级清理 | 8 |
| 四、三级清理 | 4.3 三级清理 | 7 |
| 五、回收预估 | 4.1/4.2/4.3 预估 | — |
| 六、注意事项 | 七、安全与容错 | — |

### B. 预估回收汇总（沿用原数据）

| 级别 | 预估回收 | 风险 | 本文档确认机制 |
|------|---------|------|----------------|
| 一级 | ~50 GB | 无风险 | 简单确认 |
| 二级 | ~85 GB | 低风险 | 强确认 + 需勾选了解 |
| 三级 | ~15 GB | 中风险 | 最强确认 + 输入 IDE 名 |
| **合计** | **~150 GB** | | |

### C. 已确认事项（2026-09-04）

| 事项 | 决定 |
|------|------|
| 产品形态 | ✅ 桌面 GUI（Tauri + SolidJS） |
| 目标磁盘线 | ✅ 沿用 100GB 固定值 |
| 自动重启应用 | ❌ 不需要，清理后不重启任何应用 |
| 定时/CI 集成 | ❌ 不需要，不做定时任务，先做手动触发 |

---

## 十一、P0 实现报告（2026-09-04）

P0 已完成并**编译运行通过**。覆盖范围：主菜单 + 四级菜单 + 一级清理全功能 + 实时反馈 + 诊断总览 + 设置。

### 11.1 已实现功能

| 模块 | 文件 | 说明 |
|------|------|------|
| 命令注册表 | `src-tauri/src/cleanup.rs` | 31 个清理动作全部结构化（ID/等级/风险/预估/扫描路径/执行命令） |
| 磁盘诊断 | `src-tauri/src/cleanup.rs` | `df -k /` 解析 + `du` Top 排行 |
| 执行引擎 | `src-tauri/src/cleanup.rs` | 清理前/后实时测量，释放空间精确到 0.1 GB |
| 交互式命令 | `src-tauri/src/cleanup.rs` | NVM / Rustup 版本列表与卸载 |
| IDE 在用检测 | `src-tauri/src/cleanup.rs` | `pgrep` 检测，三级清理安全锁 |
| 路由 | `src/components/Router.tsx` | 4 个视图切换 |
| 状态栏 | `src/components/StatusBar.tsx` + `DiskOverview.tsx` | 磁盘总览、目标线警告、累计释放 |
| 主菜单 | `src/components/MenuGrid.tsx` | 四级菜单卡片 |
| 菜单视图 | `src/components/MenuView.tsx` | 勾选/全选/一键批量/反馈区 |
| 单个命令 | `src/components/ActionItem.tsx` + `ResultBadge.tsx` | 三级确认强度 + 实时反馈 |
| 确认弹窗 | `src/components/ConfirmDialog.tsx` | simple/strong/strongest 三级 |
| 交互式弹窗 | `src/components/InteractiveView.tsx` | NVM/Rustup 版本选择 |
| 诊断视图 | `src/components/DiagView.tsx` + `DiagReport.tsx` | 只读扫描 + 导出 |
| 设置视图 | `src/components/SettingsView.tsx` | 排除列表（持久化）、日志导出 |

### 11.2 验证结果

- ✅ `cargo build` — Rust 后端编译通过
- ✅ `vite build` — 前端构建通过（23 modules transformed）
- ✅ `tsc --noEmit` — TypeScript 全量类型检查零错误
- ✅ `tauri dev` — Vite ready + Tauri Finished + Devtools 加载，应用初始化正常

### 11.3 关键技术决策

1. **构建目标 safari13 → safari15**：esbuild 0.27.7 完全不支持 safari13 的 destructuring 转换（实测确认），而 SolidJS 信号 idioms 依赖解构。safari15 是最小可用修复，Tauri 2 的 WebKit 远新于此，无兼容风险。
2. **规避 `Show` 子回调的 accessor 歧义**：SolidJS `Show` 的子回调在运行时可能传入 accessor 函数而非原始值（与文档表述不一致，已读运行时源码确认）。全部改为「子组件 + 直接传值」模式，children 为元素而非函数，彻底规避歧义且保持响应式。`For` 经 `mapArray` 运行时确认传值，安全。
3. **命令结构化**：所有清理动作为数据而非裸字符串，确认弹窗、风险标签、预估大小、实时反馈均可自动生成。

### 11.4 待办（P1/P2/P3）

- [ ] P1：二级清理完整交互（NVM/Rustup 已实现 UI，待联调）
- [ ] P2：三级清理 IDE 在用检测联动（`check_ide_in_use_cmd` 已就绪）
- [ ] P3：批量执行汇总确认弹窗、日志查看器、关于页

### 11.5 磁盘诊断修复记录（2026-09-04）

**问题**：启动后默认显示的磁盘已用量明显不对（显示 11GB used）。

**根因**：macOS APFS 把 `/` 拆成独立的**系统卷**（`/dev/disk3s3s1`，只读、很小）与**数据卷**（`/System/Volumes/Data`）。原代码执行 `df -k /` 只拿到系统卷，而用户 Home、`~/Library/Caches` 等实际都在数据卷——真实状态是 **904.7 GB used / 971.4 GB total / 93%**，可用仅 15.9GB，这才是卡顿真凶。

**修复**：
1. `get_disk_usage()` 改为对 **home 目录所在卷**查询 `df -k "$HOME"`，拿到真实数据卷。
2. `usage_pct` 从读取 macOS Capacity 列改为计算 `used/total`——Capacity 列口径是 `used/(used+available)`（不含预留块），会与界面「已用/总量」文字打架，导致进度条和文字对不上。
3. 解析逻辑抽成纯函数 `parse_df_k`，并加单元测试覆盖 macOS 数据卷格式与空输入，防止回归。

**验证**：`cargo test --lib` 2/2 通过；`df -k ~` 实测数据卷 904.7/971.4 GB。

### 11.6 目标线改为可配置（2026-09-04）

**问题**：原设计目标线固定 100GB，但不同机器磁盘总量差异巨大（256GB / 512GB / 1TB / 2TB），固定值没有意义。

**实现**：
- 新增 `src/store.ts`：`diskTargetGb` 响应式信号 + `setDiskTargetGb()`，持久化到 `localStorage`，范围钳制在 1–2000 GB
- 消费方 `DiskOverview`、`DiagReport`、`DiagView`（导出文案）全部改为读取 store，去掉常量 `DISK_TARGET_GB`
- 设置页目标线从「静态展示」改为「可编辑数字输入框」，支持回车或失焦应用，非法值自动回退

**效果**：用户按自己磁盘总量设目标线（如 512GB 机器设 300GB），状态栏、诊断报告的「超出/达标」判断与进度条全部联动。

### 11.7 目标线输入响应式修复（2026-09-04）

**问题**：设置页修改目标线后，状态栏展示的目标值不变。

**根因**：输入框用了独立的本地信号 `targetInput`，必须按 Enter 或失焦才调用 `setDiskTargetGb` 写入 store。用户输入后若未触发应用步骤，store 未更新，所有消费方（状态栏、诊断报告）自然保持旧值——表现为"改了但展示不变"。

**修复**：输入框改为**直接绑定 store 信号**的受控组件：
- `value={diskTargetGb()}` — 读取来源是 store
- `onInput` — 每次按键直接调用 `setDiskTargetGb` 写 store，即时生效、自动持久化

这样消除"应用"步骤，任何按键都立即传播到全部消费方，无 stale 状态。空输入时跳过写入，避免清空时跳到默认值。

**验证**：`tsc --noEmit` 零错误；`vite build` 通过；构建产物确认 `DiskOverview` 在组件 body 中响应式读取目标信号（`n=se()`），消费方与 store 同一信号实例。

### 11.8 macOS `input type="number"` IME 报错修复（2026-09-04）

**现象**：修改目标线输入框后，控制台报 `error messaging the mach port for IMKCFRunLoopWakeUpReliable`。

**根因**：这是 macOS 上 `<input type="number">` 的经典 IME（InputMethodKit）bug——数字输入框在受控更新时会与 IME 的 CFRunLoop 通信冲突，导致 mach port 消息失败。Tauri/Chromium 内嵌 WebKit 上尤其常见。

**修复**：输入框从 `type="number"` 改为 `type="text"` + `inputMode="numeric"` + `pattern="[0-9]*"`：
- 键盘仍弹数字键盘（`inputMode` 保证）
- 规避了 `type="number"` 的 IME run loop 冲突
- 保留受控绑定 store 的即时生效行为

**验证**：全仓无其他 `type="number"` 输入框；`tsc --noEmit` 零错误；`vite build` 通过。

### 11.9 IME 报错彻底修复（2026-09-04）

**现象**：改为 `type="text"` 后重启 `tauri dev`，修改目标线数值仍报 `error messaging the mach port for IMKCFRunLoopWakeUpReliable`。

**根因（更深一层）**：`type="text"` 规避了数字输入框的 IME 冲突，但真正触发 mach port 报错的是**程序化改动输入框的值**。原先「每次按键写 store → store 变了又通过 `value={diskTargetGb()}` 回写输入框」的受控绑定，会在 store 值与用户输入不一致（如输入 3000 被钳到 2000）时强制改写 DOM 的 `value`，命中 macOS IME 的雷区。

**修复**：改为**标准表单模式**（输入与 store 解耦）：
- 输入框绑定**本地信号** `localTarget`，只在用户输入时更新，store **不回写**输入框（SolidJS 设值前会比对，值相同则跳过 DOM 写入，不会程序化改动）
- 仅在**回车 / 失焦 / 点击「应用」**时 `commitTarget()` 提交到 store
- 附带可见的「应用」按钮，避免"改了没提交"的歧义

**效果**：输入框的值不再被 store 反向覆盖，macOS IME run loop 不再被异常唤醒，报错消失；同时保留了"提交后状态栏实时联动"的行为。

### 11.10 IME 报错终极修复：完全非受控输入（2026-09-04）

**现象**：改为本地信号受控绑定后，重启 `tauri dev` 仍报 `error messaging the mach port for IMKCFRunLoopWakeUpReliable`，并伴随 `NSSpellServer ... timed out` 与 `TSM AdjustCapsLockLED ... Inhibit`。

**根因（终极定位）**：SolidJS 的 `value={x}` 受控绑定，**每次渲染都会调用 `el.value = x`**，即便值与 DOM 当前值相同也不会跳过。这个程序化写入本身就会打扰 macOS 的 IME / TSM / NSSpellServer 的 run loop，触发 mach port 报错。本地信号受控绑定仍命中此路径，因为每次按键都会触发一次 `el.value` 写入。

**修复**：目标线输入框改为**完全非受控**：
- 去掉 `value` / `defaultValue` 属性，仅用 `ref` 拿到 DOM 元素
- 初始值在 `onMount` 中通过 `el.value = ...` 设置一次（仅首次）
- 用户输入由浏览器直接管理，SolidJS **从不**再碰 `el.value`
- 仅在回车 / 失焦 / 点击「应用」时用 `el.value` 读取并提交到 store

**效果**：输入框的 `el.value` 只被写入两次（onMount 首次、用户清空时回填），其余时间完全由浏览器管理，macOS 输入系统不再被异常唤醒，三连报错消失。同时保留提交后状态栏实时联动。

**验证**：构建产物确认输入框无 `value` 属性（`<input type=text inputmode=numeric pattern=[0-9]* ...>`）；`tsc --noEmit` 零错误；`vite build` 通过。